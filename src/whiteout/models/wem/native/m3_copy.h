// SPDX-License-Identifier: BSD-3-Clause
// Copyright (c) 2026 Fernando Sahmkow
//
// GENERATED by tools/codegen (--backend wem-native). Do not edit.
//
// The mechanical half of the m3 material converter: every field that maps
// 1:1 between the parser record and its WEM mirror. Fields that do not map —
// resolved references, version-gated meanings — carry `@wem manual`, are absent
// here, and are named in `native::kM3ManualFields` so the converter's
// tests can assert nobody forgot one.
//
// Private to src/ on purpose: this is the one generated file that includes a
// parser header, and §7.3's whole argument is that the public WEM headers do not.

#pragma once

#include <cstddef>

#include <whiteout/models/m3/structures.h>
#include <whiteout/models/m3/structures/anim.h>
#include <whiteout/models/m3/structures/base.h>
#include <whiteout/models/m3/structures/effect.h>
#include <whiteout/models/m3/structures/material.h>
#include <whiteout/models/m3/structures/mesh.h>
#include <whiteout/models/m3/structures/misc.h>
#include <whiteout/models/m3/structures/physics.h>
#include <whiteout/models/m3/structures/scene.h>
#include <whiteout/models/m3/types.h>
#include <whiteout/models/wem/native/m3_native.h>

namespace whiteout {
namespace models {
namespace wem {

inline void CopyToNative(const whiteout::m3::ColorBGRA& src, native::M3ColorBGRA& dst);
inline void CopyFromNative(const native::M3ColorBGRA& src, whiteout::m3::ColorBGRA& dst);
inline void CopyToNative(const whiteout::m3::TextureLayer& src, native::M3TextureLayer& dst);
inline void CopyFromNative(const native::M3TextureLayer& src, whiteout::m3::TextureLayer& dst);
inline void CopyToNative(const whiteout::m3::StandardMaterial& src, native::M3Standard& dst);
inline void CopyFromNative(const native::M3Standard& src, whiteout::m3::StandardMaterial& dst);
inline void CopyToNative(const whiteout::m3::DisplacementMaterial& src,
                         native::M3Displacement& dst);
inline void CopyFromNative(const native::M3Displacement& src,
                           whiteout::m3::DisplacementMaterial& dst);
inline void CopyToNative(const whiteout::m3::CompositeSection& src,
                         native::M3CompositeSection& dst);
inline void CopyFromNative(const native::M3CompositeSection& src,
                           whiteout::m3::CompositeSection& dst);
inline void CopyToNative(const whiteout::m3::CompositeMaterial& src, native::M3Composite& dst);
inline void CopyFromNative(const native::M3Composite& src, whiteout::m3::CompositeMaterial& dst);
inline void CopyToNative(const whiteout::m3::TerrainMaterial& src, native::M3Terrain& dst);
inline void CopyFromNative(const native::M3Terrain& src, whiteout::m3::TerrainMaterial& dst);
inline void CopyToNative(const whiteout::m3::VolumeMaterial& src, native::M3Volume& dst);
inline void CopyFromNative(const native::M3Volume& src, whiteout::m3::VolumeMaterial& dst);
inline void CopyToNative(const whiteout::m3::VolumeNoiseMaterial& src, native::M3VolumeNoise& dst);
inline void CopyFromNative(const native::M3VolumeNoise& src,
                           whiteout::m3::VolumeNoiseMaterial& dst);
inline void CopyToNative(const whiteout::m3::CreepMaterial& src, native::M3Creep& dst);
inline void CopyFromNative(const native::M3Creep& src, whiteout::m3::CreepMaterial& dst);
inline void CopyToNative(const whiteout::m3::STBMaterial& src, native::M3Stb& dst);
inline void CopyFromNative(const native::M3Stb& src, whiteout::m3::STBMaterial& dst);
inline void CopyToNative(const whiteout::m3::ReflectionMaterial& src, native::M3Reflection& dst);
inline void CopyFromNative(const native::M3Reflection& src, whiteout::m3::ReflectionMaterial& dst);
inline void CopyToNative(const whiteout::m3::SubFlare& src, native::M3SubFlare& dst);
inline void CopyFromNative(const native::M3SubFlare& src, whiteout::m3::SubFlare& dst);
inline void CopyToNative(const whiteout::m3::LensFlare& src, native::M3LensFlare& dst);
inline void CopyFromNative(const native::M3LensFlare& src, whiteout::m3::LensFlare& dst);
inline void CopyToNative(const whiteout::m3::DataDrivenMaterial& src, native::M3DataDriven& dst);
inline void CopyFromNative(const native::M3DataDriven& src, whiteout::m3::DataDrivenMaterial& dst);
inline void CopyToNative(const whiteout::m3::DataDrivenProperty& src,
                         native::M3DataDrivenProperty& dst);
inline void CopyFromNative(const native::M3DataDrivenProperty& src,
                           whiteout::m3::DataDrivenProperty& dst);
inline void CopyToNative(const whiteout::m3::DataDrivenGroup& src, native::M3DataDrivenGroup& dst);
inline void CopyFromNative(const native::M3DataDrivenGroup& src,
                           whiteout::m3::DataDrivenGroup& dst);
inline void CopyToNative(const whiteout::m3::DataDrivenProperties& src,
                         native::M3DataDrivenProperties& dst);
inline void CopyFromNative(const native::M3DataDrivenProperties& src,
                           whiteout::m3::DataDrivenProperties& dst);

// Identity copy, so a mirrored class template's body can say
// `CopyToNative(x, y)` without knowing whether its value type is a scalar
// (same type both sides) or a mirrored struct (its own overload, below).
template <class T>
inline void CopyToNative(const T& src, T& dst) {
    dst = src;
}

template <class T>
inline void CopyFromNative(const T& src, T& dst) {
    dst = src;
}

template <class S, class D>
inline void CopyToNative(const whiteout::m3::AnimRef<S>& src, native::M3AnimRef<D>& dst) {
    dst.interpType = src.interpType;
    dst.flags = src.flags;
    dst.animId = src.animId;
    CopyToNative(src.initValue, dst.initValue);
    CopyToNative(src.nullValue, dst.nullValue);
    dst.unused = src.unused;
}

template <class S, class D>
inline void CopyFromNative(const native::M3AnimRef<S>& src, whiteout::m3::AnimRef<D>& dst) {
    dst.interpType = src.interpType;
    dst.flags = src.flags;
    dst.animId = src.animId;
    CopyFromNative(src.initValue, dst.initValue);
    CopyFromNative(src.nullValue, dst.nullValue);
    dst.unused = src.unused;
}

inline void CopyToNative(const whiteout::m3::ColorBGRA& src, native::M3ColorBGRA& dst) {
    dst.b = src.b;
    dst.g = src.g;
    dst.r = src.r;
    dst.a = src.a;
}

inline void CopyFromNative(const native::M3ColorBGRA& src, whiteout::m3::ColorBGRA& dst) {
    dst.b = src.b;
    dst.g = src.g;
    dst.r = src.r;
    dst.a = src.a;
}

inline void CopyToNative(const whiteout::m3::TextureLayer& src, native::M3TextureLayer& dst) {
    dst.id = src.id;
    dst.texturePath = src.texturePath;
    CopyToNative(src.color, dst.color);
    dst.flags = static_cast<native::M3TextureLayerFlag>(static_cast<u32>(src.flags));
    dst.uvMapping = static_cast<native::M3UVMappingMode>(static_cast<u32>(src.uvMapping));
    dst.colorType = static_cast<native::M3ColorChannelSelect>(static_cast<u32>(src.colorType));
    CopyToNative(src.rgbMultiply, dst.rgbMultiply);
    CopyToNative(src.rgbAdd, dst.rgbAdd);
    dst.pocTexture = src.pocTexture;
    dst.noiseAmplitude = src.noiseAmplitude;
    dst.noiseFrequency = src.noiseFrequency;
    dst.textureSource = src.textureSource;
    dst.aviFrameRate = src.aviFrameRate;
    dst.aviStart = src.aviStart;
    dst.aviStop = src.aviStop;
    dst.aviLoop = src.aviLoop;
    dst.aviSync = src.aviSync;
    CopyToNative(src.aviPlay, dst.aviPlay);
    CopyToNative(src.aviRestart, dst.aviRestart);
    dst.flipbookRows = src.flipbookRows;
    dst.flipbookColumns = src.flipbookColumns;
    CopyToNative(src.currentFrame, dst.currentFrame);
    CopyToNative(src.uvOffset, dst.uvOffset);
    CopyToNative(src.uvAngle, dst.uvAngle);
    CopyToNative(src.uvTiling, dst.uvTiling);
    CopyToNative(src.wOffset, dst.wOffset);
    CopyToNative(src.wTiling, dst.wTiling);
    CopyToNative(src.mapAlpha, dst.mapAlpha);
    CopyToNative(src.triplanarOffset, dst.triplanarOffset);
    CopyToNative(src.triplanarScale, dst.triplanarScale);
    dst.uvSourceRelated = src.uvSourceRelated;
    dst.fresnelMode = static_cast<native::M3FresnelMode>(static_cast<u32>(src.fresnelMode));
    dst.fresnelExponent = src.fresnelExponent;
    dst.fresnelMin = src.fresnelMin;
    dst.fresnelMax = src.fresnelMax;
    dst.fresnelTranslation = src.fresnelTranslation;
    dst.fresnelMask = src.fresnelMask;
    dst.fresnelRotation = src.fresnelRotation;
    dst.uvDensity = src.uvDensity;
}

inline void CopyFromNative(const native::M3TextureLayer& src, whiteout::m3::TextureLayer& dst) {
    dst.id = src.id;
    dst.texturePath = src.texturePath;
    CopyFromNative(src.color, dst.color);
    dst.flags = static_cast<whiteout::m3::TextureLayerFlag>(static_cast<u32>(src.flags));
    dst.uvMapping = static_cast<whiteout::m3::UVMappingMode>(static_cast<u32>(src.uvMapping));
    dst.colorType = static_cast<whiteout::m3::ColorChannelSelect>(static_cast<u32>(src.colorType));
    CopyFromNative(src.rgbMultiply, dst.rgbMultiply);
    CopyFromNative(src.rgbAdd, dst.rgbAdd);
    dst.pocTexture = src.pocTexture;
    dst.noiseAmplitude = src.noiseAmplitude;
    dst.noiseFrequency = src.noiseFrequency;
    dst.textureSource = src.textureSource;
    dst.aviFrameRate = src.aviFrameRate;
    dst.aviStart = src.aviStart;
    dst.aviStop = src.aviStop;
    dst.aviLoop = src.aviLoop;
    dst.aviSync = src.aviSync;
    CopyFromNative(src.aviPlay, dst.aviPlay);
    CopyFromNative(src.aviRestart, dst.aviRestart);
    dst.flipbookRows = src.flipbookRows;
    dst.flipbookColumns = src.flipbookColumns;
    CopyFromNative(src.currentFrame, dst.currentFrame);
    CopyFromNative(src.uvOffset, dst.uvOffset);
    CopyFromNative(src.uvAngle, dst.uvAngle);
    CopyFromNative(src.uvTiling, dst.uvTiling);
    CopyFromNative(src.wOffset, dst.wOffset);
    CopyFromNative(src.wTiling, dst.wTiling);
    CopyFromNative(src.mapAlpha, dst.mapAlpha);
    CopyFromNative(src.triplanarOffset, dst.triplanarOffset);
    CopyFromNative(src.triplanarScale, dst.triplanarScale);
    dst.uvSourceRelated = src.uvSourceRelated;
    dst.fresnelMode = static_cast<whiteout::m3::FresnelMode>(static_cast<u32>(src.fresnelMode));
    dst.fresnelExponent = src.fresnelExponent;
    dst.fresnelMin = src.fresnelMin;
    dst.fresnelMax = src.fresnelMax;
    dst.fresnelTranslation = src.fresnelTranslation;
    dst.fresnelMask = src.fresnelMask;
    dst.fresnelRotation = src.fresnelRotation;
    dst.uvDensity = src.uvDensity;
}

inline void CopyToNative(const whiteout::m3::StandardMaterial& src, native::M3Standard& dst) {
    dst.name = src.name;
    dst.additionalFlags =
        static_cast<native::M3MaterialAdditionalFlag>(static_cast<u32>(src.additionalFlags));
    dst.flags = static_cast<native::M3MaterialFlag>(static_cast<u32>(src.flags));
    dst.blendMode = static_cast<native::M3BlendMode>(static_cast<u32>(src.blendMode));
    dst.priority = src.priority;
    dst.rttChannels = src.rttChannels;
    dst.specularExponent = src.specularExponent;
    dst.depthBlendFalloff = src.depthBlendFalloff;
    dst.alphaTestThreshold = src.alphaTestThreshold;
    dst.hdrSpecularMultiplier = src.hdrSpecularMultiplier;
    dst.hdrEmissiveMultiplier = src.hdrEmissiveMultiplier;
    dst.hdrEnvironmentConstant = src.hdrEnvironmentConstant;
    dst.hdrEnvironmentDiffuse = src.hdrEnvironmentDiffuse;
    dst.hdrEnvironmentSpecular = src.hdrEnvironmentSpecular;
    if (src.diffuseLayer.has_value()) {
        dst.diffuseLayer.emplace();
        const auto& src_opt = src.diffuseLayer;
        auto& dst_opt = dst.diffuseLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.diffuseLayer.reset();
    }
    if (src.decalLayer.has_value()) {
        dst.decalLayer.emplace();
        const auto& src_opt = src.decalLayer;
        auto& dst_opt = dst.decalLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.decalLayer.reset();
    }
    if (src.specularLayer.has_value()) {
        dst.specularLayer.emplace();
        const auto& src_opt = src.specularLayer;
        auto& dst_opt = dst.specularLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.specularLayer.reset();
    }
    if (src.glossLayer.has_value()) {
        dst.glossLayer.emplace();
        const auto& src_opt = src.glossLayer;
        auto& dst_opt = dst.glossLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.glossLayer.reset();
    }
    if (src.emissiveLayer1.has_value()) {
        dst.emissiveLayer1.emplace();
        const auto& src_opt = src.emissiveLayer1;
        auto& dst_opt = dst.emissiveLayer1;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.emissiveLayer1.reset();
    }
    if (src.emissiveLayer2.has_value()) {
        dst.emissiveLayer2.emplace();
        const auto& src_opt = src.emissiveLayer2;
        auto& dst_opt = dst.emissiveLayer2;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.emissiveLayer2.reset();
    }
    if (src.environmentLayer.has_value()) {
        dst.environmentLayer.emplace();
        const auto& src_opt = src.environmentLayer;
        auto& dst_opt = dst.environmentLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.environmentLayer.reset();
    }
    if (src.environmentMaskLayer.has_value()) {
        dst.environmentMaskLayer.emplace();
        const auto& src_opt = src.environmentMaskLayer;
        auto& dst_opt = dst.environmentMaskLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.environmentMaskLayer.reset();
    }
    if (src.alphaLayer1.has_value()) {
        dst.alphaLayer1.emplace();
        const auto& src_opt = src.alphaLayer1;
        auto& dst_opt = dst.alphaLayer1;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.alphaLayer1.reset();
    }
    if (src.alphaLayer2.has_value()) {
        dst.alphaLayer2.emplace();
        const auto& src_opt = src.alphaLayer2;
        auto& dst_opt = dst.alphaLayer2;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.alphaLayer2.reset();
    }
    if (src.normalLayer.has_value()) {
        dst.normalLayer.emplace();
        const auto& src_opt = src.normalLayer;
        auto& dst_opt = dst.normalLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalLayer.reset();
    }
    if (src.heightLayer.has_value()) {
        dst.heightLayer.emplace();
        const auto& src_opt = src.heightLayer;
        auto& dst_opt = dst.heightLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.heightLayer.reset();
    }
    if (src.lightMapLayer.has_value()) {
        dst.lightMapLayer.emplace();
        const auto& src_opt = src.lightMapLayer;
        auto& dst_opt = dst.lightMapLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.lightMapLayer.reset();
    }
    if (src.ambientOcclusionLayer.has_value()) {
        dst.ambientOcclusionLayer.emplace();
        const auto& src_opt = src.ambientOcclusionLayer;
        auto& dst_opt = dst.ambientOcclusionLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.ambientOcclusionLayer.reset();
    }
    if (src.normalBlend1MaskLayer.has_value()) {
        dst.normalBlend1MaskLayer.emplace();
        const auto& src_opt = src.normalBlend1MaskLayer;
        auto& dst_opt = dst.normalBlend1MaskLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend1MaskLayer.reset();
    }
    if (src.normalBlend2MaskLayer.has_value()) {
        dst.normalBlend2MaskLayer.emplace();
        const auto& src_opt = src.normalBlend2MaskLayer;
        auto& dst_opt = dst.normalBlend2MaskLayer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend2MaskLayer.reset();
    }
    if (src.normalBlend1Layer.has_value()) {
        dst.normalBlend1Layer.emplace();
        const auto& src_opt = src.normalBlend1Layer;
        auto& dst_opt = dst.normalBlend1Layer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend1Layer.reset();
    }
    if (src.normalBlend2Layer.has_value()) {
        dst.normalBlend2Layer.emplace();
        const auto& src_opt = src.normalBlend2Layer;
        auto& dst_opt = dst.normalBlend2Layer;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend2Layer.reset();
    }
    dst.materialClass = static_cast<native::M3MaterialClass>(static_cast<u32>(src.materialClass));
    dst.layerBlendMode = static_cast<native::M3LayerBlendOp>(static_cast<u32>(src.layerBlendMode));
    dst.emissiveBlendMode1 =
        static_cast<native::M3LayerBlendOp>(static_cast<u32>(src.emissiveBlendMode1));
    dst.emissiveBlendMode2 =
        static_cast<native::M3LayerBlendOp>(static_cast<u32>(src.emissiveBlendMode2));
    dst.specularMode = static_cast<native::M3SpecularMode>(static_cast<u32>(src.specularMode));
    CopyToNative(src.parallaxHeight, dst.parallaxHeight);
    CopyToNative(src.motionBlurAmount, dst.motionBlurAmount);
    {
        const auto& src_v = src.normalBlendFactors;
        auto& dst_v = dst.normalBlendFactors;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyToNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyFromNative(const native::M3Standard& src, whiteout::m3::StandardMaterial& dst) {
    dst.name = src.name;
    dst.additionalFlags =
        static_cast<whiteout::m3::MaterialAdditionalFlag>(static_cast<u32>(src.additionalFlags));
    dst.flags = static_cast<whiteout::m3::MaterialFlag>(static_cast<u32>(src.flags));
    dst.blendMode = static_cast<whiteout::m3::BlendMode>(static_cast<u32>(src.blendMode));
    dst.priority = src.priority;
    dst.rttChannels = src.rttChannels;
    dst.specularExponent = src.specularExponent;
    dst.depthBlendFalloff = src.depthBlendFalloff;
    dst.alphaTestThreshold = src.alphaTestThreshold;
    dst.hdrSpecularMultiplier = src.hdrSpecularMultiplier;
    dst.hdrEmissiveMultiplier = src.hdrEmissiveMultiplier;
    dst.hdrEnvironmentConstant = src.hdrEnvironmentConstant;
    dst.hdrEnvironmentDiffuse = src.hdrEnvironmentDiffuse;
    dst.hdrEnvironmentSpecular = src.hdrEnvironmentSpecular;
    if (src.diffuseLayer.has_value()) {
        dst.diffuseLayer.emplace();
        const auto& src_opt = src.diffuseLayer;
        auto& dst_opt = dst.diffuseLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.diffuseLayer.reset();
    }
    if (src.decalLayer.has_value()) {
        dst.decalLayer.emplace();
        const auto& src_opt = src.decalLayer;
        auto& dst_opt = dst.decalLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.decalLayer.reset();
    }
    if (src.specularLayer.has_value()) {
        dst.specularLayer.emplace();
        const auto& src_opt = src.specularLayer;
        auto& dst_opt = dst.specularLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.specularLayer.reset();
    }
    if (src.glossLayer.has_value()) {
        dst.glossLayer.emplace();
        const auto& src_opt = src.glossLayer;
        auto& dst_opt = dst.glossLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.glossLayer.reset();
    }
    if (src.emissiveLayer1.has_value()) {
        dst.emissiveLayer1.emplace();
        const auto& src_opt = src.emissiveLayer1;
        auto& dst_opt = dst.emissiveLayer1;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.emissiveLayer1.reset();
    }
    if (src.emissiveLayer2.has_value()) {
        dst.emissiveLayer2.emplace();
        const auto& src_opt = src.emissiveLayer2;
        auto& dst_opt = dst.emissiveLayer2;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.emissiveLayer2.reset();
    }
    if (src.environmentLayer.has_value()) {
        dst.environmentLayer.emplace();
        const auto& src_opt = src.environmentLayer;
        auto& dst_opt = dst.environmentLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.environmentLayer.reset();
    }
    if (src.environmentMaskLayer.has_value()) {
        dst.environmentMaskLayer.emplace();
        const auto& src_opt = src.environmentMaskLayer;
        auto& dst_opt = dst.environmentMaskLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.environmentMaskLayer.reset();
    }
    if (src.alphaLayer1.has_value()) {
        dst.alphaLayer1.emplace();
        const auto& src_opt = src.alphaLayer1;
        auto& dst_opt = dst.alphaLayer1;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.alphaLayer1.reset();
    }
    if (src.alphaLayer2.has_value()) {
        dst.alphaLayer2.emplace();
        const auto& src_opt = src.alphaLayer2;
        auto& dst_opt = dst.alphaLayer2;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.alphaLayer2.reset();
    }
    if (src.normalLayer.has_value()) {
        dst.normalLayer.emplace();
        const auto& src_opt = src.normalLayer;
        auto& dst_opt = dst.normalLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalLayer.reset();
    }
    if (src.heightLayer.has_value()) {
        dst.heightLayer.emplace();
        const auto& src_opt = src.heightLayer;
        auto& dst_opt = dst.heightLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.heightLayer.reset();
    }
    if (src.lightMapLayer.has_value()) {
        dst.lightMapLayer.emplace();
        const auto& src_opt = src.lightMapLayer;
        auto& dst_opt = dst.lightMapLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.lightMapLayer.reset();
    }
    if (src.ambientOcclusionLayer.has_value()) {
        dst.ambientOcclusionLayer.emplace();
        const auto& src_opt = src.ambientOcclusionLayer;
        auto& dst_opt = dst.ambientOcclusionLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.ambientOcclusionLayer.reset();
    }
    if (src.normalBlend1MaskLayer.has_value()) {
        dst.normalBlend1MaskLayer.emplace();
        const auto& src_opt = src.normalBlend1MaskLayer;
        auto& dst_opt = dst.normalBlend1MaskLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend1MaskLayer.reset();
    }
    if (src.normalBlend2MaskLayer.has_value()) {
        dst.normalBlend2MaskLayer.emplace();
        const auto& src_opt = src.normalBlend2MaskLayer;
        auto& dst_opt = dst.normalBlend2MaskLayer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend2MaskLayer.reset();
    }
    if (src.normalBlend1Layer.has_value()) {
        dst.normalBlend1Layer.emplace();
        const auto& src_opt = src.normalBlend1Layer;
        auto& dst_opt = dst.normalBlend1Layer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend1Layer.reset();
    }
    if (src.normalBlend2Layer.has_value()) {
        dst.normalBlend2Layer.emplace();
        const auto& src_opt = src.normalBlend2Layer;
        auto& dst_opt = dst.normalBlend2Layer;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalBlend2Layer.reset();
    }
    dst.materialClass =
        static_cast<whiteout::m3::MaterialClass>(static_cast<u32>(src.materialClass));
    dst.layerBlendMode =
        static_cast<whiteout::m3::LayerBlendOp>(static_cast<u32>(src.layerBlendMode));
    dst.emissiveBlendMode1 =
        static_cast<whiteout::m3::LayerBlendOp>(static_cast<u32>(src.emissiveBlendMode1));
    dst.emissiveBlendMode2 =
        static_cast<whiteout::m3::LayerBlendOp>(static_cast<u32>(src.emissiveBlendMode2));
    dst.specularMode = static_cast<whiteout::m3::SpecularMode>(static_cast<u32>(src.specularMode));
    CopyFromNative(src.parallaxHeight, dst.parallaxHeight);
    CopyFromNative(src.motionBlurAmount, dst.motionBlurAmount);
    {
        const auto& src_v = src.normalBlendFactors;
        auto& dst_v = dst.normalBlendFactors;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyFromNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyToNative(const whiteout::m3::DisplacementMaterial& src,
                         native::M3Displacement& dst) {
    dst.name = src.name;
    dst.unknown = src.unknown;
    CopyToNative(src.strength, dst.strength);
    if (src.normalMap.has_value()) {
        dst.normalMap.emplace();
        const auto& src_opt = src.normalMap;
        auto& dst_opt = dst.normalMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalMap.reset();
    }
    if (src.strengthMap.has_value()) {
        dst.strengthMap.emplace();
        const auto& src_opt = src.strengthMap;
        auto& dst_opt = dst.strengthMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.strengthMap.reset();
    }
    dst.flags = src.flags.value;
    dst.priority = src.priority;
}

inline void CopyFromNative(const native::M3Displacement& src,
                           whiteout::m3::DisplacementMaterial& dst) {
    dst.name = src.name;
    dst.unknown = src.unknown;
    CopyFromNative(src.strength, dst.strength);
    if (src.normalMap.has_value()) {
        dst.normalMap.emplace();
        const auto& src_opt = src.normalMap;
        auto& dst_opt = dst.normalMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalMap.reset();
    }
    if (src.strengthMap.has_value()) {
        dst.strengthMap.emplace();
        const auto& src_opt = src.strengthMap;
        auto& dst_opt = dst.strengthMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.strengthMap.reset();
    }
    dst.flags.value = src.flags;
    dst.priority = src.priority;
}

inline void CopyToNative(const whiteout::m3::CompositeSection& src,
                         native::M3CompositeSection& dst) {
    dst.materialIndex = src.materialIndex;
    CopyToNative(src.mapMultiplier, dst.mapMultiplier);
}

inline void CopyFromNative(const native::M3CompositeSection& src,
                           whiteout::m3::CompositeSection& dst) {
    dst.materialIndex = src.materialIndex;
    CopyFromNative(src.mapMultiplier, dst.mapMultiplier);
}

inline void CopyToNative(const whiteout::m3::CompositeMaterial& src, native::M3Composite& dst) {
    dst.name = src.name;
    dst.priority = src.priority;
    {
        const auto& src_v = src.sections;
        auto& dst_v = dst.sections;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyToNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyFromNative(const native::M3Composite& src, whiteout::m3::CompositeMaterial& dst) {
    dst.name = src.name;
    dst.priority = src.priority;
    {
        const auto& src_v = src.sections;
        auto& dst_v = dst.sections;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyFromNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyToNative(const whiteout::m3::TerrainMaterial& src, native::M3Terrain& dst) {
    dst.name = src.name;
    if (src.terrainMap.has_value()) {
        dst.terrainMap.emplace();
        const auto& src_opt = src.terrainMap;
        auto& dst_opt = dst.terrainMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.terrainMap.reset();
    }
    dst.unknown = src.unknown;
}

inline void CopyFromNative(const native::M3Terrain& src, whiteout::m3::TerrainMaterial& dst) {
    dst.name = src.name;
    if (src.terrainMap.has_value()) {
        dst.terrainMap.emplace();
        const auto& src_opt = src.terrainMap;
        auto& dst_opt = dst.terrainMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.terrainMap.reset();
    }
    dst.unknown = src.unknown;
}

inline void CopyToNative(const whiteout::m3::VolumeMaterial& src, native::M3Volume& dst) {
    dst.name = src.name;
    dst.blendMode = src.blendMode;
    dst.falloffType = static_cast<native::M3VolumeFalloffType>(static_cast<u32>(src.falloffType));
    CopyToNative(src.density, dst.density);
    if (src.colorMap.has_value()) {
        dst.colorMap.emplace();
        const auto& src_opt = src.colorMap;
        auto& dst_opt = dst.colorMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.colorMap.reset();
    }
    if (src.noiseMap1.has_value()) {
        dst.noiseMap1.emplace();
        const auto& src_opt = src.noiseMap1;
        auto& dst_opt = dst.noiseMap1;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap1.reset();
    }
    if (src.noiseMap2.has_value()) {
        dst.noiseMap2.emplace();
        const auto& src_opt = src.noiseMap2;
        auto& dst_opt = dst.noiseMap2;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap2.reset();
    }
    dst.alphaThreshold = src.alphaThreshold;
    dst.flags = src.flags.value;
}

inline void CopyFromNative(const native::M3Volume& src, whiteout::m3::VolumeMaterial& dst) {
    dst.name = src.name;
    dst.blendMode = src.blendMode;
    dst.falloffType =
        static_cast<whiteout::m3::VolumeFalloffType>(static_cast<u32>(src.falloffType));
    CopyFromNative(src.density, dst.density);
    if (src.colorMap.has_value()) {
        dst.colorMap.emplace();
        const auto& src_opt = src.colorMap;
        auto& dst_opt = dst.colorMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.colorMap.reset();
    }
    if (src.noiseMap1.has_value()) {
        dst.noiseMap1.emplace();
        const auto& src_opt = src.noiseMap1;
        auto& dst_opt = dst.noiseMap1;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap1.reset();
    }
    if (src.noiseMap2.has_value()) {
        dst.noiseMap2.emplace();
        const auto& src_opt = src.noiseMap2;
        auto& dst_opt = dst.noiseMap2;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap2.reset();
    }
    dst.alphaThreshold = src.alphaThreshold;
    dst.flags.value = src.flags;
}

inline void CopyToNative(const whiteout::m3::VolumeNoiseMaterial& src, native::M3VolumeNoise& dst) {
    dst.name = src.name;
    dst.falloffType = static_cast<native::M3VolumeFalloffType>(static_cast<u32>(src.falloffType));
    dst.drawTransparency =
        static_cast<native::M3VolumeNoiseCameraMode>(static_cast<u32>(src.drawTransparency));
    CopyToNative(src.density, dst.density);
    CopyToNative(src.nearPlane, dst.nearPlane);
    CopyToNative(src.falloff, dst.falloff);
    if (src.colorMap.has_value()) {
        dst.colorMap.emplace();
        const auto& src_opt = src.colorMap;
        auto& dst_opt = dst.colorMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.colorMap.reset();
    }
    if (src.noiseMap1.has_value()) {
        dst.noiseMap1.emplace();
        const auto& src_opt = src.noiseMap1;
        auto& dst_opt = dst.noiseMap1;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap1.reset();
    }
    if (src.noiseMap2.has_value()) {
        dst.noiseMap2.emplace();
        const auto& src_opt = src.noiseMap2;
        auto& dst_opt = dst.noiseMap2;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap2.reset();
    }
    CopyToNative(src.scrollRate, dst.scrollRate);
    CopyToNative(src.position, dst.position);
    CopyToNative(src.scale, dst.scale);
    CopyToNative(src.rotation, dst.rotation);
    dst.alphaThreshold = src.alphaThreshold;
    dst.flags = static_cast<native::M3VolumeNoiseMaterialFlag>(static_cast<u32>(src.flags));
}

inline void CopyFromNative(const native::M3VolumeNoise& src,
                           whiteout::m3::VolumeNoiseMaterial& dst) {
    dst.name = src.name;
    dst.falloffType =
        static_cast<whiteout::m3::VolumeFalloffType>(static_cast<u32>(src.falloffType));
    dst.drawTransparency =
        static_cast<whiteout::m3::VolumeNoiseCameraMode>(static_cast<u32>(src.drawTransparency));
    CopyFromNative(src.density, dst.density);
    CopyFromNative(src.nearPlane, dst.nearPlane);
    CopyFromNative(src.falloff, dst.falloff);
    if (src.colorMap.has_value()) {
        dst.colorMap.emplace();
        const auto& src_opt = src.colorMap;
        auto& dst_opt = dst.colorMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.colorMap.reset();
    }
    if (src.noiseMap1.has_value()) {
        dst.noiseMap1.emplace();
        const auto& src_opt = src.noiseMap1;
        auto& dst_opt = dst.noiseMap1;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap1.reset();
    }
    if (src.noiseMap2.has_value()) {
        dst.noiseMap2.emplace();
        const auto& src_opt = src.noiseMap2;
        auto& dst_opt = dst.noiseMap2;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.noiseMap2.reset();
    }
    CopyFromNative(src.scrollRate, dst.scrollRate);
    CopyFromNative(src.position, dst.position);
    CopyFromNative(src.scale, dst.scale);
    CopyFromNative(src.rotation, dst.rotation);
    dst.alphaThreshold = src.alphaThreshold;
    dst.flags = static_cast<whiteout::m3::VolumeNoiseMaterialFlag>(static_cast<u32>(src.flags));
}

inline void CopyToNative(const whiteout::m3::CreepMaterial& src, native::M3Creep& dst) {
    dst.name = src.name;
    if (src.maskMap.has_value()) {
        dst.maskMap.emplace();
        const auto& src_opt = src.maskMap;
        auto& dst_opt = dst.maskMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.maskMap.reset();
    }
    dst.creepLow = src.creepLow;
}

inline void CopyFromNative(const native::M3Creep& src, whiteout::m3::CreepMaterial& dst) {
    dst.name = src.name;
    if (src.maskMap.has_value()) {
        dst.maskMap.emplace();
        const auto& src_opt = src.maskMap;
        auto& dst_opt = dst.maskMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.maskMap.reset();
    }
    dst.creepLow = src.creepLow;
}

inline void CopyToNative(const whiteout::m3::STBMaterial& src, native::M3Stb& dst) {
    dst.name = src.name;
    if (src.diffuseMap.has_value()) {
        dst.diffuseMap.emplace();
        const auto& src_opt = src.diffuseMap;
        auto& dst_opt = dst.diffuseMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.diffuseMap.reset();
    }
    if (src.normalMap.has_value()) {
        dst.normalMap.emplace();
        const auto& src_opt = src.normalMap;
        auto& dst_opt = dst.normalMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.normalMap.reset();
    }
    if (src.specularMap.has_value()) {
        dst.specularMap.emplace();
        const auto& src_opt = src.specularMap;
        auto& dst_opt = dst.specularMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.specularMap.reset();
    }
}

inline void CopyFromNative(const native::M3Stb& src, whiteout::m3::STBMaterial& dst) {
    dst.name = src.name;
    if (src.diffuseMap.has_value()) {
        dst.diffuseMap.emplace();
        const auto& src_opt = src.diffuseMap;
        auto& dst_opt = dst.diffuseMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.diffuseMap.reset();
    }
    if (src.normalMap.has_value()) {
        dst.normalMap.emplace();
        const auto& src_opt = src.normalMap;
        auto& dst_opt = dst.normalMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.normalMap.reset();
    }
    if (src.specularMap.has_value()) {
        dst.specularMap.emplace();
        const auto& src_opt = src.specularMap;
        auto& dst_opt = dst.specularMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.specularMap.reset();
    }
}

inline void CopyToNative(const whiteout::m3::ReflectionMaterial& src, native::M3Reflection& dst) {
    dst.name = src.name;
    dst.unknown = src.unknown;
    CopyToNative(src.reflectionStrength, dst.reflectionStrength);
    CopyToNative(src.displacementStrength, dst.displacementStrength);
    CopyToNative(src.reflectionOffset, dst.reflectionOffset);
    CopyToNative(src.blurAngle, dst.blurAngle);
    CopyToNative(src.blurDistanceMax, dst.blurDistanceMax);
    if (src.reflectionMap.has_value()) {
        dst.reflectionMap.emplace();
        const auto& src_opt = src.reflectionMap;
        auto& dst_opt = dst.reflectionMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.reflectionMap.reset();
    }
    if (src.displacementMap.has_value()) {
        dst.displacementMap.emplace();
        const auto& src_opt = src.displacementMap;
        auto& dst_opt = dst.displacementMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.displacementMap.reset();
    }
    if (src.blurMap.has_value()) {
        dst.blurMap.emplace();
        const auto& src_opt = src.blurMap;
        auto& dst_opt = dst.blurMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.blurMap.reset();
    }
    dst.flags = static_cast<native::M3ReflectionMaterialFlag>(static_cast<u32>(src.flags));
    dst.unknown2 = src.unknown2;
}

inline void CopyFromNative(const native::M3Reflection& src, whiteout::m3::ReflectionMaterial& dst) {
    dst.name = src.name;
    dst.unknown = src.unknown;
    CopyFromNative(src.reflectionStrength, dst.reflectionStrength);
    CopyFromNative(src.displacementStrength, dst.displacementStrength);
    CopyFromNative(src.reflectionOffset, dst.reflectionOffset);
    CopyFromNative(src.blurAngle, dst.blurAngle);
    CopyFromNative(src.blurDistanceMax, dst.blurDistanceMax);
    if (src.reflectionMap.has_value()) {
        dst.reflectionMap.emplace();
        const auto& src_opt = src.reflectionMap;
        auto& dst_opt = dst.reflectionMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.reflectionMap.reset();
    }
    if (src.displacementMap.has_value()) {
        dst.displacementMap.emplace();
        const auto& src_opt = src.displacementMap;
        auto& dst_opt = dst.displacementMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.displacementMap.reset();
    }
    if (src.blurMap.has_value()) {
        dst.blurMap.emplace();
        const auto& src_opt = src.blurMap;
        auto& dst_opt = dst.blurMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.blurMap.reset();
    }
    dst.flags = static_cast<whiteout::m3::ReflectionMaterialFlag>(static_cast<u32>(src.flags));
    dst.unknown2 = src.unknown2;
}

inline void CopyToNative(const whiteout::m3::SubFlare& src, native::M3SubFlare& dst) {
    dst.index = src.index;
    dst.position = src.position;
    dst.sizeXY = src.sizeXY;
    dst.scaleXY = src.scaleXY;
    dst.fadeIn = src.fadeIn;
    dst.fadeOut = src.fadeOut;
    CopyToNative(src.colorAlpha, dst.colorAlpha);
    dst.faceCenter = src.faceCenter;
    dst.offset = src.offset;
}

inline void CopyFromNative(const native::M3SubFlare& src, whiteout::m3::SubFlare& dst) {
    dst.index = src.index;
    dst.position = src.position;
    dst.sizeXY = src.sizeXY;
    dst.scaleXY = src.scaleXY;
    dst.fadeIn = src.fadeIn;
    dst.fadeOut = src.fadeOut;
    CopyFromNative(src.colorAlpha, dst.colorAlpha);
    dst.faceCenter = src.faceCenter;
    dst.offset = src.offset;
}

inline void CopyToNative(const whiteout::m3::LensFlare& src, native::M3LensFlare& dst) {
    dst.name = src.name;
    if (src.flareMap.has_value()) {
        dst.flareMap.emplace();
        const auto& src_opt = src.flareMap;
        auto& dst_opt = dst.flareMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.flareMap.reset();
    }
    if (src.maskMap.has_value()) {
        dst.maskMap.emplace();
        const auto& src_opt = src.maskMap;
        auto& dst_opt = dst.maskMap;
        CopyToNative(*src_opt, *dst_opt);
    } else {
        dst.maskMap.reset();
    }
    {
        const auto& src_v = src.subFlares;
        auto& dst_v = dst.subFlares;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyToNative(src_v[i], dst_v[i]);
        }
    }
    dst.columns = src.columns;
    dst.rows = src.rows;
    dst.distanceFade = src.distanceFade;
    dst.libName = src.libName;
    CopyToNative(src.intensity, dst.intensity);
    CopyToNative(src.color, dst.color);
    CopyToNative(src.hdr, dst.hdr);
    CopyToNative(src.size, dst.size);
}

inline void CopyFromNative(const native::M3LensFlare& src, whiteout::m3::LensFlare& dst) {
    dst.name = src.name;
    if (src.flareMap.has_value()) {
        dst.flareMap.emplace();
        const auto& src_opt = src.flareMap;
        auto& dst_opt = dst.flareMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.flareMap.reset();
    }
    if (src.maskMap.has_value()) {
        dst.maskMap.emplace();
        const auto& src_opt = src.maskMap;
        auto& dst_opt = dst.maskMap;
        CopyFromNative(*src_opt, *dst_opt);
    } else {
        dst.maskMap.reset();
    }
    {
        const auto& src_v = src.subFlares;
        auto& dst_v = dst.subFlares;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyFromNative(src_v[i], dst_v[i]);
        }
    }
    dst.columns = src.columns;
    dst.rows = src.rows;
    dst.distanceFade = src.distanceFade;
    dst.libName = src.libName;
    CopyFromNative(src.intensity, dst.intensity);
    CopyFromNative(src.color, dst.color);
    CopyFromNative(src.hdr, dst.hdr);
    CopyFromNative(src.size, dst.size);
}

inline void CopyToNative(const whiteout::m3::DataDrivenMaterial& src, native::M3DataDriven& dst) {
    dst.materialName = src.materialName;
    {
        const auto& src_v = src.fragmentHashes;
        auto& dst_v = dst.fragmentHashes;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.extraHashes;
        auto& dst_v = dst.extraHashes;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.propertyBlob;
        auto& dst_v = dst.propertyBlob;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.texturePaths;
        auto& dst_v = dst.texturePaths;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    dst.unknown108 = src.unknown108;
    dst.unknown112 = src.unknown112;
    dst.unknown116 = src.unknown116;
    dst.effectNameHash = src.effectNameHash;
    dst.unknown124 = src.unknown124;
    dst.padding128 = src.padding128;
    dst.unknown132 = src.unknown132;
    dst.unknown136 = src.unknown136;
    dst.unknown140 = src.unknown140;
    dst.unknown144 = src.unknown144;
    dst.unknown148 = src.unknown148;
    dst.alphaFresnelFlags = src.alphaFresnelFlags;
    dst.shaderType = static_cast<native::M3MaterialShaderType>(static_cast<u32>(src.shaderType));
    dst.unknown151 = src.unknown151;
    dst.effectNameHash2 = src.effectNameHash2;
    dst.effectNameHash3 = src.effectNameHash3;
}

inline void CopyFromNative(const native::M3DataDriven& src, whiteout::m3::DataDrivenMaterial& dst) {
    dst.materialName = src.materialName;
    {
        const auto& src_v = src.fragmentHashes;
        auto& dst_v = dst.fragmentHashes;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.extraHashes;
        auto& dst_v = dst.extraHashes;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.propertyBlob;
        auto& dst_v = dst.propertyBlob;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    {
        const auto& src_v = src.texturePaths;
        auto& dst_v = dst.texturePaths;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
    dst.unknown108 = src.unknown108;
    dst.unknown112 = src.unknown112;
    dst.unknown116 = src.unknown116;
    dst.effectNameHash = src.effectNameHash;
    dst.unknown124 = src.unknown124;
    dst.padding128 = src.padding128;
    dst.unknown132 = src.unknown132;
    dst.unknown136 = src.unknown136;
    dst.unknown140 = src.unknown140;
    dst.unknown144 = src.unknown144;
    dst.unknown148 = src.unknown148;
    dst.alphaFresnelFlags = src.alphaFresnelFlags;
    dst.shaderType =
        static_cast<whiteout::m3::MaterialShaderType>(static_cast<u32>(src.shaderType));
    dst.unknown151 = src.unknown151;
    dst.effectNameHash2 = src.effectNameHash2;
    dst.effectNameHash3 = src.effectNameHash3;
}

inline void CopyToNative(const whiteout::m3::DataDrivenProperty& src,
                         native::M3DataDrivenProperty& dst) {
    dst.nameHash = src.nameHash;
    dst.name = src.name;
    {
        const auto& src_v = src.data;
        auto& dst_v = dst.data;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
}

inline void CopyFromNative(const native::M3DataDrivenProperty& src,
                           whiteout::m3::DataDrivenProperty& dst) {
    dst.nameHash = src.nameHash;
    dst.name = src.name;
    {
        const auto& src_v = src.data;
        auto& dst_v = dst.data;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            dst_v[i] = src_v[i];
        }
    }
}

inline void CopyToNative(const whiteout::m3::DataDrivenGroup& src, native::M3DataDrivenGroup& dst) {
    dst.nameHash = src.nameHash;
    dst.name = src.name;
    {
        const auto& src_v = src.properties;
        auto& dst_v = dst.properties;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyToNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyFromNative(const native::M3DataDrivenGroup& src,
                           whiteout::m3::DataDrivenGroup& dst) {
    dst.nameHash = src.nameHash;
    dst.name = src.name;
    {
        const auto& src_v = src.properties;
        auto& dst_v = dst.properties;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyFromNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyToNative(const whiteout::m3::DataDrivenProperties& src,
                         native::M3DataDrivenProperties& dst) {
    {
        const auto& src_v = src.groups;
        auto& dst_v = dst.groups;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyToNative(src_v[i], dst_v[i]);
        }
    }
}

inline void CopyFromNative(const native::M3DataDrivenProperties& src,
                           whiteout::m3::DataDrivenProperties& dst) {
    {
        const auto& src_v = src.groups;
        auto& dst_v = dst.groups;
        dst_v.resize(src_v.size());
        for (std::size_t i = 0; i < src_v.size(); ++i) {
            CopyFromNative(src_v[i], dst_v[i]);
        }
    }
}

} // namespace wem
} // namespace models
} // namespace whiteout
